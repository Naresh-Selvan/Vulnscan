# Copy to allowlist.yaml and edit before running any scan.
# The tool will refuse to run against anything not listed here.

# If true, localhost/127.0.0.1/this VM's hostname is always allowed
# (use this for "scan the box I'm running on" engagements).
local_only: true

# Additional explicit targets (hostnames or IPs) you have written
# authorization to test, e.g. from a bug bounty program's scope page.
targets:
  # - target.example.com
  # - 10.0.0.5
